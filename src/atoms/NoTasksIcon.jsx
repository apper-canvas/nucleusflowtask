import React from 'react';
import ApperIcon from '../ApperIcon';

const NoTasksIcon = ({ className = 'w-8 h-8 mx-auto mb-2 opacity-50' }) => (
  <ApperIcon name="Package" className={`${className}`} />
);

export default NoTasksIcon;